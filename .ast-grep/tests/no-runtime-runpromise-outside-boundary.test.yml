id: no-runtime-runpromise-outside-boundary
valid:
  - |
    const output = effect;
invalid:
  - |
    const output = Effect.runPromise(effect);
  - |
    const output = Effect.runPromiseExit(effect);
