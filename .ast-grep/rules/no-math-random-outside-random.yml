id: no-math-random-outside-random
language: TypeScript
severity: error
message: Use injected Random service instead of Math.random in internal runtime.
ignores:
  - "**/*random*.ts"
rule:
  pattern: "Math.random()"
