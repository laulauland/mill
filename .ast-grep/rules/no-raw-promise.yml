id: no-raw-promise
language: TypeScript
severity: error
message: Raw Promise construction is disallowed; use Effect abstractions.
rule:
  any:
    - pattern: "new Promise<$T>($A)"
    - pattern: "new Promise($A)"
